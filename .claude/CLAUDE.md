# Claude Entry Point

Read `../AGENTS.md` first. It is the portable index for repository behavior rules, including pointers to `../.github/instructions/*.instructions.md` files with `applyTo` metadata. Directory-specific guidance lives in the nearest `AGENTS.md`.

Keep this file as a pointer only; do not duplicate product requirements, workflow rules, or source-code conventions here. Claude-side agents should read the relevant `.github/instructions/*.instructions.md` file for the paths they are changing.
