---
paths:
  - ".claude/CLAUDE.md"
  - "AGENTS.md"
  - "src/**/*.ts"
  - "test/**/*.ts"
  - "test/**/*.ts"
  - ".github/copilot-instructions.md"
---

> TODO: copilot (in vscode and cli) should use hierarchical AGENTS.md, include directory scoped ones (by default, and via .vscode/settings.json)
> TODO: in most cases either existing/new SKILL.md or `path` scoped .claude/rules "instruction" should be used instead of AGENT.md
> TODO: AGENTS.md is a directory based alternative to cover broader topics or provide high-level clarity to other instructions/skills/rules, but still scoped to a directory to avoid project root AGENTS.md itself.
