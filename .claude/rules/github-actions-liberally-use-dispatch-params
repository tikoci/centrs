---
paths:
  - ".github/workflows/*.yaml"
  - ".github/workflows/*.yml"
---

Workflow dispatch inputs should make hidden assumptions visible.

- Use `workflow_dispatch` for QA, release, pages, and lab workflows.
- Give dispatch inputs safe defaults and document their effect in the workflow.
- Keep `lab.yaml` for one-off or platform-specific experiments that are not regular gates yet.
