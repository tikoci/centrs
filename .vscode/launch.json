{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "bun",
			"request": "launch",
			"name": "centrs CLI help",
			"program": "${workspaceFolder}/src/cli.ts",
			"args": ["--help"],
			"cwd": "${workspaceFolder}"
		}
	]
}
