{
  "name": "centrs",
  "module": "src/index.ts",
  "type": "module",
  "private": false,
  "version": "0.1.0",
  "description": "All-in-one TypeScript library/CLI/TUI/MCP for interacting with MikroTik RouterOS devices",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {
    "@agent-lint/cli": "^0.8.1",
    "@biomejs/biome": "2.4.13",
    "@secretlint/secretlint-rule-preset-recommend": "^12.3.1",
    "@tikoci/quickchr": "^0.2.0",
    "@types/bun": "^1.3.13",
    "cloc": "^2.6.0-cloc",
    "cspell": "^10.0.0",
    "markdownlint-cli2": "^0.22.1",
    "secretlint": "^12.3.1",
    "typedoc": "^0.28.19"
  },
  "scripts": {
    "git:pre-commit": "bun run lint:git:push && echo TODO add .git/hooks to call bun run git:pre-commit",
    "git:pre-push": "bun run lint:git:push && echo TODO add .git/hooks to call bun run git:pre-push ",
    "lint": "echo TODO determine should be 'default' based on agent preference on usefulness && false",
    "lint:git:commit": "bun run lint && bun run test && echo TODO review && false",
    "lint:git:push": "bun run lint:ci && echo TODO config && false",
    "lint:ci": "echo TODO && false",
    "lint:cspell": "cspell lint .",
    "lint:markdownlint": "markdownlint-cli2 \"**/*.md\"",
    "lint:markdownlint:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "lint:typecheck": "tsc --noEmit",
    "lint:biome": "biome check .",
    "lint:fix": "biome check --write .",
    "lint:secretlint": "secretlint \"**/*\"",
    "lint:agents": "agent-lint scan .",
    "build": "echo TODO unknown at this time && false",
    "build:doc:api": "echo TODO use typedoc to generate HTML user doc for API, publish on GitHub Pages"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "optionalDependencies": {},
  "dependencies": {
    "clipanion": "^4.0.0-rc.4"
  }
}
