---
applyTo: ".github/instructions/**,docs/**/*.md,.scratch/**/*.md"
---

# Descriptive prose file names

Prefer descriptive kebab-case names for new prose markdown files so humans and agents can infer likely content from a directory listing.

Keep established conventional names such as `README.md`, `AGENTS.md`, `SECURITY.md`, etc. when those names are already the convention.
