

> CONTEXT: some tools complain without a .copilot-instructions.md but agents and .claude/** things should be use by copilot automatically - we also are explicit just make that's true using
> TODO:
> - Read `../AGENTS.md`.  Project uses `AGENTS.md` instead, this file is a pointers.
> - For skills => `.claude/skills` <-- use this instead of .github/skills
> - For instructions (rules) => `.claude/rules` <-- use this instead of `.github/instructions/**/*.md`
> - All "rules" must use `paths` (if all, use `**` to be explicit), do **not** use `applyTo` or `globs`.
> - Make sure to always scope rules (instructions) using Claude's `path` <-- CoPilot documented to respects this 
> - VSCode workspace settings explicitly forces this even though default
> TODO: verify all this since docs could be wrong/out-of-date

