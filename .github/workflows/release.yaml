name: Release
run-name: Release • ${{ github.event_name }}
on:
  workflow_dispatch:
    inputs:
      publish:
        description: Enable release placeholder
        type: boolean
        default: false
permissions:
  contents: read
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true
jobs:
  release-plan:
    name: Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Release workflow placeholder
        run: echo "Release publishing is intentionally disabled until packaging specs are implemented. publish=${{ inputs.publish }}"
