# `settings/unsafe-protocol-blocked`

A cleartext protocol (e.g. ftp) was requested without the explicit ALLOW_UNSAFE_PROTOCOLS opt-in.

## Fix

See [`docs/CONSTITUTION.md`](../../CONSTITUTION.md) for the centrs error
contract. This stub will be expanded with the typical trigger and remediation
for `settings/unsafe-protocol-blocked`.
