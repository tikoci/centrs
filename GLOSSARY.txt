# Project glossary and cspell dictionary
#
# This file is the shared source of truth for project vocabulary, short term
# definitions, and cspell dictionary entries.
#
# `%term` in prompts or notes means "look up the matching entry or comment here."
#
# CSpell dictionary syntax supported here:
# - `~word` => use the word only when case sensitivity is off
# - `+word` or `word+` => allow optional compounds
# - `-word` or `word-` => require the word to appear in a compound
# - `!word` => explicitly forbid the word
#
# Correctly spelled words may still belong here when the attached comment is the
# canonical repo-specific definition or reference for the term.
#
# Core tikoci references
# %centrs: this repository and RouterOS interaction hub.
#   https://github.com/tikoci/centrs
# %quickchr: CHR automation and test helpers used by tikoci projects.
#   https://github.com/tikoci/quickchr
# %rosetta: RouterOS docs/search grounding and knowledge-base tooling.
#   https://github.com/tikoci/rosetta
# %restraml: RouterOS REST schema and explorer tooling.
#   https://github.com/tikoci/restraml
# %lsp-routeros-ts: RouterOS parser and language-server utilities.
#   https://github.com/tikoci/lsp-routeros-ts
# Language Server Protocol — the "lsp" in lsp-routeros-ts, referenced as a
# grounding source for /console/inspect client patterns (e.g. commands/api).
lsp
# RouterOS maximum transmission unit property (e.g. mtu, actual-mtu).
mtu
# %donny: Dude/discovery grounding for RouterOS device inventories.
#   https://github.com/tikoci/donny
# %m2ir: protocol IR (WinBox, nova messages) + the declarative schema-as-data
#   approach to protocol internals; consulted for heavy protocol work.
#   https://github.com/tikoci/m2ir
# %tikbook: RouterOS notebook/documentation vocabulary used across tikoci.
#   See also https://github.com/tikoci/vscode-tikbook
# %routeros-skills: reusable RouterOS agent skills.
#   https://github.com/tikoci/routeros-skills
# %fat-chr: larger CHR image experiments and packaging.
#   https://github.com/tikoci/fat-chr
# %mikropkl: MikroTik-oriented PKL experiments and schemas.
#   https://github.com/tikoci/mikropkl
# %tikdns: tikoci DNS tooling.
#   https://github.com/tikoci/tikdns
#
# External references
# %mac-telnet: MikroTik MAC Telnet tooling by haakonnessjoen.
#   https://github.com/haakonnessjoen/mac-telnet
# %netmiko: multi-vendor SSH abstraction; reference for SSH transport patterns.
#   https://github.com/ktbyers/netmiko
# %terraform-provider-routeros: widely-used Go RouterOS provider; reference for
#   native-API behavior and provisioning idioms.
#   https://github.com/terraform-routeros/terraform-provider-routeros
# %librouteros-api: haakonnessjoen's C native-API client; cross-check for our
#   native-api codec.
#   https://github.com/haakonnessjoen/librouteros-api
# %RouterOS_Tools: third-party WinBox CDB format implementation; cross-check for
#   our CDB codec.
#   https://github.com/jabb3rd/RouterOS_Tools
# %Winbox_Protocol_Dissector: Wireshark dissector for the WinBox protocol
#   (incl. RoMON proxy); grounding for future WinBox-terminal/RoMON work.
#   https://github.com/Cisco-Talos/Winbox_Protocol_Dissector
# %btest-opensource: open btest.exe (bandwidth-test) client; possible `check`
#   feature grounding. https://github.com/samm-git/btest-opensource
Addrs
ajv
amazonq
amm
AT-SPI
awk
axuielement
backpressure
bak
bjjj
btest.exe # Mikrotik /tool/bandwidth-test client for windows
bunfig
bunx
canonicalizer
CDB
CDP
centrs
ciphertext
cli
cloc
cmp
connectionrefused
cpus
crossref
cwd
datagram
davidanson
DDNS
dialable # an endpoint a client can actually connect to (advertised AND reachable)
defconf
demux
demuxes
dogtail
donny
dotenv
DSL # domain specific language
eai
env
esbuild
ETL
eventtap
EVP
execa
fanout
fat-chr
figcaption
FlaUI
frontends
haakonnessjoen
Hammerspoon
HMAC
hostfwd
IPC # inter-process communications
iscsi
jabb3rd
kbd
KDF
keystream
KSA
KVM
LDTP
librouteros # <https://github.com/haakonnessjoen/librouteros-api>
LLDP
lsp-routeros-ts
macping
mactelnet
mactelnetd
MCP
MIB
mikropkl
MikroTik
MNDP
NAND # NAND flash memory; write-back caches file writes up to ~40s on some MikroTik boards
netmiko # [Multi-vendor library to simplify Paramiko SSH connections to network devices](https://github.com/ktbyers/netmiko)
netwatch
nullglob # bash shopt that makes an unmatched glob expand to zero words instead of the literal pattern
npk # MikroTik package format
nvme-over-tcp # supported by RouterOS **with** rose-storage.npk extra-package
OID
PII # Personally Identifiable Information
POV
PRGA
proplist
pywinauto
QML
qll # CodeQL library file extension (.qll), e.g. CleartextLoggingCustomizations.qll
qmljsdebugger
quickchr
RAML
readcdb
remotehub
retarget
restraml
romon
ros
rosetta
routeros
routeros-skills
rsc # RouterOS script / export file extension (.rsc)
SARIF # Static Analysis Results Interchange Format; CodeQL's code-scanning analysis download format
scp
scriptability
scriptable
shopt # bash builtin for toggling shell options (e.g. nullglob)
Sikuli
SikuliX
skillcheck
startswith # jq builtin filter that tests whether a string begins with a given prefix
SNMP
SPI
SRP
strawman
subarray
tcode
tcodes
TCG # QEMU Tiny Code Generator software emulation backend.
terminal-over-winbox # [Pure Python implementation of the MikroTik WinBox terminal protocol — encrypted CLI access to RouterOS devices over TCP port 8291](https://github.com/subixonfire/winbox-terminal-protocol)
tgz
tikbook
tikdns
tikoci
TIKOCI
tiktui
TLV
traceroute
tostring # jq builtin filter that renders a value as its JSON string representation
tsc
tzsp
TZSP
UDP
UIA
uvx
vcs
venv
vite
vitest
viw2
vrf
webproxy
WinAppDriver
winbox
Wireshark
workstreams
XCUI
XDG
YYYYMMDDX
zod

redactable
specced
TTL

# MAC-Telnet and native-api protocol vocabulary
# Wire-format terms from MikroTik MAC-Telnet (Håkon Nessjøen's reference
# implementation) and the RouterOS binary API.
ack
acks
beginauth
clienttype
cptype
datagrams
fromserver
Håkon
mtwei
Nessjøen
passsalt
plaindata
ptype
seskey
sessionstart

# QEMU/host networking vocabulary (mac-telnet L2 investigation)
BPF
libpcap
mcast
netdev
netdevs
NIC
SLIRP
vmnet
# feth: macOS fake-ethernet interface; sysfs: Linux /sys pseudo-filesystem —
# both read by resolveEgressMac's virtual-NIC MAC fallback (src/protocols/mac-telnet.ts).
feth
sysfs

# CI workflow shell vocabulary (.github/workflows/*.yaml step scripts)
# pipefail: `set -eo pipefail` so a tee'd `bun test` failure isn't masked;
# endgroup: GitHub `::endgroup::` log fold; funcs/gsub/img: coverage-parse and
# CHR-image terms in the gate's summary + coverage-threshold steps;
# worktree: the git worktree the qa.yaml accumulate-and-gate job uses for the
# durable qa-history append-log branch.
endgroup
funcs
gsub
img
pipefail
worktree
# HVF: macOS Hypervisor.framework (x86 CHR acceleration on Intel Macs in
# verify-extended); choco: Chocolatey, the Windows package manager (QEMU install).
HVF
choco
# CodeRabbit review bot (.coderabbit.yaml); @coderabbitai is the on-demand handle.
coderabbit
coderabbitai

# GitHub account Codex review threads are authored under (see pr-review-bots).
mobileskyfi

# centrs domain vocabulary
# %identity: the device /system/identity; a centrs CDB comment *lookup key*
#   (`identity=`) used to resolve `<router>`. Human handle, deliberately may
#   repeat across records; collisions resolve via `identity/ambiguous` + --match.
# %tips: top-level envelope channel `tips[]` for advice that is explicitly not an
#   error or warning (no username set, no CDB found). Shape `{ code, message,
#   fix? }` with `tip/*` codes. See docs/CONSTITUTION.md (Result envelope).
# %__default__: reserved CDB record (`target=__default__`) supplying fallback
#   creds/metadata. Precedence args → env → device record → __default__ →
#   built-in. Core (CLI/API/MCP); never widens the MCP allowlist.
neteng
upsert
upserts
upserted
duplicative
passwordless

# MNDP discovery vocabulary (WP-2b)
tlvs
noaddr
Hac
HnD
EADDRINUSE
EACCES
ENOTSUP
EAFNOSUPPORT
ECONNREFUSED
ECONNRESET
REUSEPORT
REUSEADDR
# L2 bridge frame-parsing terms (test/integration/mndp-l2-bridge.ts):
# ethertype: 16-bit EtherType field in an Ethernet frame (0x0800 = IPv4).
ethertype
# ihl: IPv4 Internet Header Length (low nibble of the first byte, in 32-bit words).
ihl
# PIIX: Intel PCI-ISA bridge chipset QEMU emulates; shows up in CHR's REST board-name.
PIIX

# Test-router and host-ARP vocabulary (centrs CLI fixes)
# bigdude: a live test router (MAC 96:5D:80:7D:BF:59) used to ground fixes.
bigdude
ifscope
macvlan
unpadded

# RouterOS CLI canonicalization vocabulary
subshell
disambiguator

# centrs MCP surface vocabulary
# blackhole: a RouterOS route type / bare flag used as the bench parser-truth example.
blackhole
readback
creds
# Streamable: the MCP Streamable HTTP transport.
Streamable
# modelcontextprotocol: npm scope of the MCP SDK (@modelcontextprotocol/sdk).
modelcontextprotocol
# WIP: work in progress (used for the upstream MikroTik Docusaurus docs site).
WIP
# adhoc: future inline host+credential targets; Phase 1 MCP uses CDB-only targets.
adhoc
# nonexistentattr: a deliberately invalid RouterOS attribute used to exercise the validate dual-gate.
nonexistentattr
# underflow: reading past the end of a buffer; the WinBox CDB codec reports it as a structured parse fault.
underflow
underflows

# Werror: the gcc/clang -Werror "treat warnings as errors" flag, referenced by execute --strict.
Werror

# MTWEI / EC-SRP vocabulary (src/protocols/mtwei.ts, mac-telnet auth)
# docrypto: mtwei.c's client EC-SRP proof function (mtwei_docrypto), ported to TS.
docrypto
# redp: REDP1 (IEEE P1363.2) random-point derivation used in the MTWEI tangle.
redp
keypair
# mul: camelCase fragment from scalarMul (EC scalar multiplication).
mul
# weier: camelCase fragment from xWeier (a Weierstrass-form X coordinate).
weier
# LSB: least-significant bit (Y-parity in compressed EC point encoding).
lsb
# NUL: the 0x00 byte separating username and pubkey in the MTWEI offer.
nul
# keepalive: empty-ACK idle keepalive for a held-open MAC-Telnet session.
keepalive
# eths: local var for the /interface/ethernet record list in the CHR test.
eths

# MAC-Telnet interactive-console vocabulary (src/protocols/mac-telnet-console.ts)
# DSR: ANSI Device Status Report (ESC[6n cursor-position query) the console sends.
DSR
# DECID: ANSI "identify terminal" escape (ESC Z) the console also sends.
DECID
# CSI: ANSI Control Sequence Introducer (ESC[) — the escape family we strip.
CSI
# SIGWINCH: terminal-resize signal; an interactive terminal re-reports its size on it.
SIGWINCH
# evl: RouterOS :parse renders a parsed command as "(evl ...)" on the console.
evl
# unconfigured: a freshly-flashed RouterOS device with no IP — the mac-telnet use case.
unconfigured
unwired

# MAC-Telnet reliability vocabulary (MacTelnetSession.tick: retransmit + keepalive)
# unacked: an outbound frame not yet acknowledged by the peer's byte-counter ACK.
unacked
# retransmittable: a frame (SESSIONSTART/DATA) the session resends until acked.
retransmittable
# clockless: the session keeps no real timer; tick(now) supplies time deltas.
clockless
# keepalives: plural of keepalive (empty-ACK idle frames).
keepalives

# btest / bandwidth-test vocabulary (commands/btest, src/protocols/btest.ts)
# btest: MikroTik bandwidth test peer protocol (TCP/UDP 2000); /tool/bandwidth-test
#   + /tool/bandwidth-server. centrs's "measure" capability axis.
btest
# goodput: application-level TCP throughput (excludes TCP/IP headers + retransmits),
#   how RouterOS accounts the TCP bandwidth test.
goodput
# Weierstrass: the short-Weierstrass curve form of Curve25519 used by MikroTik
#   EC-SRP5 (shared with mac-telnet MTWEI; see src/protocols/mtwei.ts / ec-srp5.ts).
Weierstrass
# ndjson: newline-delimited JSON; the stream / btest output contract (one envelope per line).
ndjson
# allocate-udp-ports-from: /tool/bandwidth-server base of the UDP data-port range.
# srp5: the "5" suffix of EC-SRP5 (the MikroTik EC-SRP variant).
srp5
# SRV: the server-endpoint test variable in commands/btest/examples.md.
srv
# firewalled: blocked by a firewall (a btest connection-refused remediation).
firewalled
# btest reference implementations + EC-SRP5 protocol vocabulary:
# manawenuz/btest-rs (Rust; both modes + EC-SRP5) and samm-git/btest-opensource
# (C; no EC-SRP5) are the wire-format references; ECPESVDP-SRP-A/B name the
# client/server EC-SRP shared-secret roles; docryptos = mtwei.c's server proof fn.
manawenuz
samm
opensource
ecsrp
ECPESVDP
docryptos
keypairs
# btest test fixtures (credentials + the "antar" username from the MITM capture).
testuser
testpass
wrongpass
antar
swordfish

# btest session layer.
pselect
interoperation
Mbps
Gbps
Kbps
trippable

# btest integration-test fixtures.
btestuser
btestpass

# RouterOS scripting builtin referenced in commands/api grounding notes
# (:toarray coerces a comma string into an array; explains the inspect path form).
toarray

# Logic shorthand ("if and only if") used in api write-gate doc comments; cspell's
# markdown dictionary knows it but the TypeScript one does not.
iff

# GitHub Pages build vocabulary (scripts/build-pages-site.ts, pages.yaml)
# typedoc/pandoc: the API-reference generator and the markdown->HTML converter
#   the Pages build chains together. gfm: GitHub-Flavored Markdown, pandoc's
#   input format for the error pages. larr: the "&larr;" left-arrow HTML entity
#   in the back-to-docs link. nojekyll: the marker file that disables GitHub
#   Pages' Jekyll processing for an Actions-deployed artifact. extensionless:
#   describes the `details_url` contract (docs/errors/README.md) — no `.html`.
typedoc
pandoc
gfm
larr
nojekyll
extensionless
# nothrow: Bun Shell's `$` method to disable throwing on a non-zero exit
# (scripts/agent-check.ts), so the caller can inspect output either way.
nothrow

# check-command spec vocabulary (commands/check/README.md, examples.md)
# TikTOML: centrs-owned readable device-inventory format (#137); check's
#   metadata feeds it. routerboard: RouterBOARD firmware/hardware menu
#   (/system/routerboard). rscmd: the tikbook RouterOS command-notebook file
#   extension (.rscmd), the format the check spec was grounded against.
TikTOML
routerboard
rscmd
# RTT: round-trip time, the host->router connect-latency reach signal. tojson: the
#   /system/script helper (:serialize to=json) the batched execute reads reuse.
#   qrcode: the /ip/cloud vpn-wireguard-client-config-qrcode field token (redacted
#   by the cloud check, referenced when documenting redaction).
RTT
tojson
qrcode

# Device GPS vocabulary (issue #146, commands/devices/README.md "Location /
# GPS", src/resolver/geo.ts). MSL/AGL: the two supported altitude-type vertical
#   datums (mean sea level / above ground level); stored case-insensitively but
#   normalized to upper case. EPSG: the geodetic coordinate-reference-system
#   registry (EPSG:4326 = WGS84 lat/lon), cited for the lat-first ordering
#   rationale. bbox: the --bbox geo selector (bounding box). wgs: WGS84, the
#   geodetic datum GPS coordinates use; appears only as a deliberately-invalid
#   --altitude-type value in tests/examples. antimeridian: the 180° meridian a
#   --bbox may not cross in v1. GEOSEARCH: the Redis command whose m/km/mi/ft
#   radius-unit vocabulary --near borrows. IUGG: the geodesy union whose mean
#   Earth radius the haversine uses. SFO/OAK: airports used as a known-distance
#   haversine fixture in tests. ele/GPX: `ele` is GPX's altitude element, kept as
#   an input alias for `altitude` (GPX = the GPS Exchange Format). Geolocation:
#   the W3C Geolocation API, cited (with ISO 6709 / EPSG / IETF geo:) for why
#   `altitude` — not `elevation` — is the canonical field name.
MSL
msl
AGL
agl
Agl
EPSG
bbox
wgs
antimeridian
GEOSEARCH
IUGG
SFO
sfo
ele
GPX
Geolocation
unparseable
Unparseable
versionless
