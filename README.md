# centrs

> TODO: explain curated/opinionated + multi-protocol (REST, SSH, API, SNMP, MNDP, MAC-telnet, WinBox Terminal) TS API + CLI + TUI + MCP Server + "proxy/daemon/server" as a "all-in-one" for agentic AI and humans needing a regularized scheme to interact with RouterOS devices, including using either dude.db or WinBox CDB file for authentications.
> TODO: add CLI reference manual <-- "spec" for CLI (as initial baseline which other specs stems like API or TUI or MCP "borrow")
> TODO: links to other generated docs
> TODO: explain all options align and similar across all interfaces: environment variables, `.env.<node_env>`, --cli-args, API parameters, MCP tools
> TODO: link to API Explorer using
> TODO: add quick dev setup, **include deps needs for windows/mac/linux**, and point to agentic ai file(s) for workflow details
